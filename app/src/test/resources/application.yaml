spring:
  datasource:
    url: jdbc:h2:mem:toplms_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect

app:
  host: localhost
  port: 0
  base-url: http://localhost:0
  email: test-admin@example.com
  password: test-password
  secret-key: test-secret-key-for-context-loading-only

demo:
  company-name: Test Academy
  subdomain: test
  plan-id: GROWTH
  name: admin
  admin-email: demo-admin@example.com
  admin-password: test-password
